# benchmark_pdfs

Local benchmark PDF corpus files live here.

This directory is intentionally ignored by Git except for this README and/or `.gitkeep`.

Some benchmark scripts and configs reference PDF filenames under `benchmark_pdfs/`. A fresh clone may need the local benchmark corpus restored separately before those benchmark scripts can be run.

Do not commit large or redistributable PDF corpus files here unless there is a deliberate licensing and repository-size decision.
