# Phase 5 eval fixtures

Tracked Phase 5 evaluation fixtures live here.

The root-level `phase5_eval/` directory is kept local/ignored for compatibility with existing local engineering workflows and product paths.
