# Oracle deployment legacy notes

This directory preserves Oracle-like / Oracle Always Free deployment documents.

The current active deployment target is AWS slim. These Oracle documents are retained as historical/deferred operational references and should not be read as the primary deployment path.

Current AWS deployment entry points remain:

- `docs/deployment/aws-slim-deploy.md`
- `docs/deployment/AWS_FRESH_EC2_BOOTSTRAP.md`
- `scripts/build_deployment_bundle.sh`
- `scripts/deploy_aws_slim.sh`
- `scripts/smoke_aws_slim.sh`
